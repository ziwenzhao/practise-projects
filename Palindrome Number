public class Solution {
    public boolean isPalindrome(int x) {
        int n,sign,ans=0;
        if(x<0){
            return false;
        }
        n=x;

        while(n!=0){
            ans=ans*10+n%10;
            n=n/10;
        }
        
        return ans==x;
    }
}
